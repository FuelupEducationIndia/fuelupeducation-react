version: '3'

services:
  client:
    container_name: client
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - /app/node_modules
      - .:/app
    ports:
      - 3000:3000

    networks:
      - api-network
networks:
  api-network:
    driver: bridge